<div>
  {{> header}}

  <section class="page">
    {{> sidebar}}

    <section class="main">
      <header class="main__header">Browse</header>

      <nav class="main__nav">
        <a class="link is-active" href="#">Overview</a>
        <a class="link" href="#">Charts</a>
        <a class="link" href="#">Genres and Moods</a>
        <a class="link" href="#">New Releases</a>
        <a class="link" href="#">Discover</a>
        <a class="link" href="#">More</a>
      </nav>

      <section class="main__content">
        <div class="album">
          <div class="album__info">
            <div class="album__info__art">
              <img src="/web/album/{{album.id}}/cover" alt="{{album.name}}" loading="lazy" />
            </div>

            <div class="album__info__data">
              <div class="album__year">{{album.year}}</div>
              <div class="album__name">{{album.name}}</div>
              <div class="album__producers">Producers: {{album.producers}}</div>
              <div class="album__studio">Studio: {{album.studio}}</div>
              <div class="album__duration">{{album.numberOfTracks}} songs • {{album.fullDuration}}</div>
            </div>
          </div>

          <div class="album__tracks">
            <div class="tracks">
              <div class="tracks__header">
                <div class="tracks__header__number">#</div>
                <div class="tracks__header__title">Song</div>
                <div class="tracks__header__length">
                  <i class="far fa-clock"></i>
                </div>
              </div>

              {{#each album.tracks}}
              <div class="track" id={{id}} onclick="OnClickMusic(this.id)">
                <div class="track__number">{{@index}}</div>
                <div class="track__title">{{title}}</div>
                <div class="track__length">{{duration}}</div>
              </div>
              {{/each}}
            </div>
          </div>
        </div>
      </section>
    </section>
  </section>

  {{> current-track}}
</div>